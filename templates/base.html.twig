<!DOCTYPE html>
<html lang="fr" data-theme="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}CoachPro{% endblock %}</title>
  <script>document.documentElement.setAttribute('data-theme', localStorage.getItem('theme') || 'dark');</script>
  {{ importmap('app') }}
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
</head>
<body class="bg-base-200 min-h-screen">

  {# ── NAVBAR ────────────────────────────────────────────────────────────────── #}
  {% set current = app.request.attributes.get('_route') %}
  <div class="navbar bg-base-100 shadow-sm px-4 mb-6 sticky top-0 z-50">

    {# Logo #}
    <div class="navbar-start">
      <a href="{{ path('app_dashboard') }}" class="btn btn-ghost gap-2 px-2 text-lg font-bold tracking-tight">
        CoachPro <span class="text-xs font-normal opacity-40 hidden sm:inline">Suivi sportif</span>
      </a>
    </div>

    {# Nav desktop #}
    <div class="navbar-center hidden lg:flex">
      <ul class="menu menu-horizontal px-1 gap-1">
        <li>
          <a href="{{ path('app_dashboard') }}" class="{{ current == 'app_dashboard' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/></svg>
            Dashboard
          </a>
        </li>
        <li>
          <a href="{{ path('app_weight_index') }}" class="{{ current starts with 'app_weight' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"/></svg>
            Poids
          </a>
        </li>
        <li>
          <a href="{{ path('app_session_index') }}" class="{{ current starts with 'app_session' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
            Séances
          </a>
        </li>
        <li>
          <a href="{{ path('app_nutrition_index') }}" class="{{ current starts with 'app_nutrition' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg>
            Nutrition
          </a>
        </li>
        <li>
          <a href="{{ path('app_analytics_index') }}" class="{{ current starts with 'app_analytics' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 8v8m-4-5v5m-4-2v2m-2 4h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>
            Stats & PRs
          </a>
        </li>
        <li>
          <a href="{{ path('app_program_show') }}" class="{{ current starts with 'app_program' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/></svg>
            Programme
          </a>
        </li>
        <li>
          <a href="{{ path('app_challenge_index') }}" class="{{ current starts with 'app_challenge' ? 'active' : '' }}">
            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4M7.835 4.697a3.42 3.42 0 001.946-.806 3.42 3.42 0 014.438 0 3.42 3.42 0 001.946.806 3.42 3.42 0 013.138 3.138 3.42 3.42 0 00.806 1.946 3.42 3.42 0 010 4.438 3.42 3.42 0 00-.806 1.946 3.42 3.42 0 01-3.138 3.138 3.42 3.42 0 00-1.946.806 3.42 3.42 0 01-4.438 0 3.42 3.42 0 00-1.946-.806 3.42 3.42 0 01-3.138-3.138 3.42 3.42 0 00-.806-1.946 3.42 3.42 0 010-4.438 3.42 3.42 0 00.806-1.946 3.42 3.42 0 013.138-3.138z"/></svg>
            Défis
          </a>
        </li>
        {% if is_granted('ROLE_COACH') %}
        <li>
          <a href="{{ path('app_coach_clients') }}" class="{{ current starts with 'app_coach' ? 'active' : '' }}">
            <i class="bi bi-people-fill"></i>
            Mes clients
          </a>
        </li>
        {% endif %}
      </ul>
    </div>

    {# Droite : thème + user #}
    <div class="navbar-end gap-2">

      {# Toggle Clair/Sombre rapide #}
      <label class="swap swap-rotate btn btn-ghost btn-circle" title="Basculer Clair/Sombre">
        <input type="checkbox" id="navbar-theme-toggle" value="light" onchange="toggleDarkLight(this)" />
        <svg class="swap-off fill-current w-5 h-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M5.64,17l-.71.71a1,1,0,0,0,0,1.41,1,1,0,0,0,1.41,0l.71-.71A1,1,0,0,0,5.64,17ZM5,12a1,1,0,0,0-1-1H3a1,1,0,0,0,0,2H4A1,1,0,0,0,5,12Zm7-7a1,1,0,0,0,1-1V3a1,1,0,0,0-2,0V4A1,1,0,0,0,12,5ZM5.64,7.05a1,1,0,0,0,.7.29,1,1,0,0,0,.71-.29,1,1,0,0,0,0-1.41l-.71-.71A1,1,0,0,0,4.93,6.34Zm12,.29a1,1,0,0,0,.7-.29l.71-.71a1,1,0,1,0-1.41-1.41L17,5.64a1,1,0,0,0,0,1.41A1,1,0,0,0,17.66,7.34ZM21,11H20a1,1,0,0,0,0,2h1a1,1,0,0,0,0-2Zm-9,8a1,1,0,0,0-1,1v1a1,1,0,0,0,2,0V20A1,1,0,0,0,12,19ZM18.36,17A1,1,0,0,0,17,18.36l.71.71a1,1,0,0,0,1.41,0,1,1,0,0,0,0-1.41ZM12,6.5A5.5,5.5,0,1,0,17.5,12,5.51,5.51,0,0,0,12,6.5Zm0,9A3.5,3.5,0,1,1,15.5,12,3.5,3.5,0,0,1,12,15.5Z"/></svg>
        <svg class="swap-on fill-current w-5 h-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24"><path d="M21.64,13a1,1,0,0,0-1.05-.14,8.05,8.05,0,0,1-3.37.73A8.15,8.15,0,0,1,9.08,5.49a8.59,8.59,0,0,1,.25-2A1,1,0,0,0,8,2.36,10.14,10.14,0,1,0,22,14.05,1,1,0,0,0,21.64,13Zm-9.5,6.69A8.14,8.14,0,0,1,7.08,5.22v.27A10.15,10.15,0,0,0,17.22,15.63a9.79,9.79,0,0,0,2.1-.22A8.11,8.11,0,0,1,12.14,19.73Z"/></svg>
      </label>

      {# Dropdown user #}
      {% if app.user %}
        <div class="dropdown dropdown-end">
          <div tabindex="0" role="button" class="btn btn-ghost btn-circle avatar placeholder">
            <div class="bg-primary text-primary-content rounded-full w-9">
              <span class="text-sm font-bold">{{ app.user.firstName ? app.user.firstName|first|upper : app.user.email|first|upper }}</span>
            </div>
          </div>
          <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-50 w-52 p-2 shadow-lg mt-2 border border-base-300">
            <li class="menu-title">
              <span class="text-xs truncate">{{ app.user.firstName }} {{ app.user.lastName }}</span>
              <span class="text-xs opacity-40 truncate">{{ app.user.email }}</span>
            </li>
            <li>
              <a href="{{ path('app_profile') }}" class="{{ current starts with 'app_profile' ? 'active' : '' }}">
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"/></svg>
                Mon Profil
              </a>
            </li>
            <li><hr class="my-1 border-base-200"></li>
            <li>
              <a href="{{ path('app_logout') }}" class="text-error">
                <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/></svg>
                Se déconnecter
              </a>
            </li>
          </ul>
        </div>
      {% endif %}

      {# Burger mobile #}
      <div class="dropdown dropdown-end lg:hidden">
        <div tabindex="0" role="button" class="btn btn-ghost btn-circle">
          <svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
        </div>
        <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box z-50 w-56 p-2 shadow-lg mt-2 border border-base-300">
          <li><a href="{{ path('app_dashboard') }}" class="{{ current == 'app_dashboard' ? 'active' : '' }}">Dashboard</a></li>
          <li><a href="{{ path('app_weight_index') }}" class="{{ current starts with 'app_weight' ? 'active' : '' }}">Poids</a></li>
          <li><a href="{{ path('app_session_index') }}" class="{{ current starts with 'app_session' ? 'active' : '' }}">Séances</a></li>
          <li><a href="{{ path('app_nutrition_index') }}" class="{{ current starts with 'app_nutrition' ? 'active' : '' }}">Nutrition</a></li>
          <li><a href="{{ path('app_analytics_index') }}" class="{{ current starts with 'app_analytics' ? 'active' : '' }}">Stats & PRs</a></li>
          <li><a href="{{ path('app_program_show') }}" class="{{ current starts with 'app_program' ? 'active' : '' }}">Programme</a></li>
          <li><a href="{{ path('app_challenge_index') }}" class="{{ current starts with 'app_challenge' ? 'active' : '' }}">Défis 🔥</a></li>
          {% if is_granted('ROLE_COACH') %}<li><a href="{{ path('app_coach_clients') }}" class="{{ current starts with 'app_coach' ? 'active' : '' }}">Mes clients</a></li>{% endif %}
          <li><hr class="my-1 border-base-200"></li>
          <li><a href="{{ path('app_profile') }}">Mon Profil</a></li>
          <li><a href="{{ path('app_logout') }}" class="text-error">Déconnexion</a></li>
        </ul>
      </div>
    </div>
  </div>

  {# ── CONTENU PRINCIPAL ─────────────────────────────────────────────────────── #}
  <main class="container mx-auto px-4 pb-8 max-w-7xl">

    {# Flash messages #}
    {% for type, messages in app.flashes %}
      {% for message in messages %}
        <div class="alert alert-{{ type == 'success' ? 'success' : (type == 'error' ? 'error' : 'info') }} shadow-sm mb-4">
          <span>{{ message }}</span>
        </div>
      {% endfor %}
    {% endfor %}

    {% block body %}{% endblock %}
  </main>

  <script>
    function setTheme(theme) {
      document.documentElement.setAttribute('data-theme', theme);
      localStorage.setItem('theme', theme);
      
      // Mettre à jour l'état du toggle dans la navbar
      const toggle = document.getElementById('navbar-theme-toggle');
      if (toggle) {
        toggle.checked = (theme === 'light');
      }
    }
    
    function toggleDarkLight(checkbox) {
      const newTheme = checkbox.checked ? 'light' : 'dark';
      setTheme(newTheme);
    }
    
    (function() {
      var current = localStorage.getItem('theme') || 'dark';
      var toggle = document.getElementById('navbar-theme-toggle');
      if (toggle && current === 'light') toggle.checked = true;
    })();
  </script>

  {# ── CHAT IA FLOTTANT ─────────────────────────────────────────────────────── #}
  {{ include('_chat_widget.html.twig') }}

{#
  DaisyUI theme safelist — Tailwind scanner anchor, do not remove.
  Tailwind v4 scans templates as plain text: even inside a Twig comment,
  data-theme values are detected and DaisyUI generates the corresponding CSS.
  data-theme="valentine" data-theme="business" data-theme="nord"
  data-theme="corporate" data-theme="cupcake" data-theme="dracula"
  data-theme="cyberpunk" data-theme="synthwave" data-theme="luxury"
  data-theme="night" data-theme="dim" data-theme="sunset" data-theme="forest"
#}
</body>
</html>
